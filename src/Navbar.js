import React from 'react';

function Navbar() {
    return (
        <div>
            <button>Home</button>
            <button>CARS</button>
            <button>GALLERY</button>

        </div>
    )
}

export default Navbar;
